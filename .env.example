# ============================================
# Cluttr - Environment Configuration
# ============================================
# Copy this file to .env and configure the values below

# --------------------------------------------
# OAuth Configuration
# --------------------------------------------
# Google OAuth iOS Client ID (for iOS builds)
# Get this from Google Cloud Console
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com

# Google OAuth Android Client ID (for Android builds)
# Get this from Google Cloud Console
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your-android-client-id.apps.googleusercontent.com

# Firebase web OAuth client ID (required by GoogleSignin.configure webClientId)
# Google Cloud Console -> Credentials -> "Web client (auto created by Google Service)"
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com

# --------------------------------------------
# Logging Configuration
# --------------------------------------------
# Log level controls the verbosity of logging output
# Available levels (in order of verbosity):
#   silent  - No logging output
#   error   - Only errors are logged
#   warn    - Warnings and errors
#   info    - Info, warnings, and errors (default)
#   debug   - Debug, info, warnings, and errors
#   verbose - Maximum detail including all traces
EXPO_PUBLIC_LOG_LEVEL=info

# Log categories filter (comma-separated list, or * for all)
# This allows you to enable logging only for specific parts of the app
# Available categories:
#   api        - API client requests/responses
#   sync       - Sync service operations
#   auth       - Authentication operations
#   storage    - Local storage/file operations
#   navigation - Navigation events
#   ui         - UI events and interactions
#   redux      - Redux state changes
#   saga       - Redux saga operations
#   network    - Network status and connectivity
#   image      - Image processing and uploads
#   ai         - AI recognition operations
#   general    - General logging
#
# Examples:
#   EXPO_PUBLIC_LOG_CATEGORIES=*                    # All categories (default)
#   EXPO_PUBLIC_LOG_CATEGORIES=api,sync             # Only API and sync
#   EXPO_PUBLIC_LOG_CATEGORIES=sync,auth,storage    # Specific categories
EXPO_PUBLIC_LOG_CATEGORIES=*

# Show timestamps in log messages
# Set to 'true' to include timestamps in each log line
EXPO_PUBLIC_LOG_TIMESTAMPS=true

# Show emojis in log messages
# Set to 'true' to include emoji icons for easy visual identification
# Set to 'false' if your terminal doesn't support emojis well
EXPO_PUBLIC_LOG_EMOJIS=true

# --------------------------------------------
# Common Development Configurations
# --------------------------------------------
# For development with detailed logging:
# EXPO_PUBLIC_LOG_LEVEL=verbose
# EXPO_PUBLIC_LOG_CATEGORIES=*

# For debugging sync issues only:
# EXPO_PUBLIC_LOG_LEVEL=debug
# EXPO_PUBLIC_LOG_CATEGORIES=sync,api

# For debugging API issues only:
# EXPO_PUBLIC_LOG_LEVEL=debug
# EXPO_PUBLIC_LOG_CATEGORIES=api,auth

# For production (minimal logging):
# EXPO_PUBLIC_LOG_LEVEL=error
# EXPO_PUBLIC_LOG_CATEGORIES=*
